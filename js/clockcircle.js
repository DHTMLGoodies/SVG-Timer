/**
 * Created by alfmagne1 on 11/08/16.
 */
DG.ClockCircle = function(config){
    this.svg = config.svg;
    // { x, y, width, height }
    this.bounds = config.bounds;

    this.render();
};


$.extend(DG.ClockCircle.prototype, {


});